import styles from "./Partners.module.css";

const Partners = (props) => {
  return (
    <div className={styles.outerDiv}>
      <div className={styles.innerDiv}>I will be a page of partners</div>
    </div>
  );
};

export default Partners;
